# Required — Anthropic API key for LLM intent extraction
ANTHROPIC_API_KEY=

# Required for RunPod deployment — GitHub PAT (repo:read scope)
# Used by setup.sh to clone the repo on pod start
RUNPOD_GITHUB_TOKEN=
